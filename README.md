
# Chit Chat App  Add commentMore actions

A real-time chat application built using the **MERN stack** with **Socket.IO** for instant messaging. It includes authentication, theme customization, profile picture updates, online user status, and image sharing.  

## Features  
- 🔐 **Authentication** (Sign up, login, and logout)  
- 🎨 **Theme Customization** (30+ themes)  
- 👥 **Online User Toggle** (See who's online)  
- 📸 **Profile Picture Updates**  
- 📝 **Real-time Messaging** with **Socket.IO**  
- 🖼️ **Image Sharing** in chat  
- 🚀 **Smooth UI** with **DaisyUI & Tailwind CSS**  

## Technologies Used  
- **Frontend:** React, Tailwind CSS, DaisyUI, React Hot Toast, React Lucide Icons  
- **Backend:** Node.js, Express, MongoDB, Socket.IO  
- **Authentication:** JWT (JSON Web Tokens)  
- **Deployment:** Render  

## Installation & Setup  

1. **Clone the repository:**  
   ```sh
   git clone https://github.com/your-username/chit-chat-app.git
   cd chit-chat-app
   ```

2. **Install dependencies:**  
   - **Frontend:**  
     ```sh
     cd client
     npm install
     ```
   - **Backend:**  
     ```sh
     cd server
     npm install
     ```

3. **Set up environment variables:**  
   Create a `.env` file in the **server** directory and add:  
   ```
   MONGO_URI=your_mongodb_connection_string
   JWT_SECRET=your_secret_key
   ```

4. **Run the application:**  
   - **Backend:**  
     ```sh
     cd server
     npm start
     ```
   - **Frontend:**  
     ```sh
     cd client
     npm start
     ```

5. **Open the app in your browser:**  
   ```
   http://localhost:3000
   ```

## Deployment  
- **Frontend & Backend deployed on Render**  

## Contributing  
Feel free to contribute by opening issues or submitting pull requests.  Add comment
